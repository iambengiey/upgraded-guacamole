# IT Services Rwanda static site

Single-page marketing site for **IT Services Rwanda** built with HTML, CSS, and vanilla JavaScript.

## Local preview
Open `index.html` directly in a browser or serve the folder with a simple HTTP server.

## Deployment
GitHub Actions (`.github/workflows/deploy.yml`) automatically builds and deploys the static site to GitHub Pages on pushes to `main` or after a pull request review is approved.
